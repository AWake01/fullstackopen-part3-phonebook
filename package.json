{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "FullStackOpen exercise 3.1 phonebook backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd R:/fullstackopen/fullstackopen_course/'part 2 Communicating with server'/exercises/phonebook && npm run build && cp -r dist R:/fullstackopen/'part 3 - Programming a server with NodeJS and Express - exercises'/fullstackopen-part3-phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Sandy Wake",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongodb": "^6.17.0",
    "mongoose": "^8.16.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.30.1",
    "globals": "^16.3.0"
  }
}
